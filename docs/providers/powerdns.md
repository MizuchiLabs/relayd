# PowerDNS

```env
RELAYD_PROVIDER_PDNS_TYPE=powerdns
RELAYD_PROVIDER_PDNS_URL=http://your-pdns-api:8081
RELAYD_PROVIDER_PDNS_TOKEN=your-api-key
RELAYD_PROVIDER_PDNS_ZONES=example.com
```

- **Scope**: `local` (typically used for internal zones)
- **Requires**: PowerDNS API key and URL.
