# Hetzner

```env
RELAYD_PROVIDER_HETZNER_TYPE=hetzner
RELAYD_PROVIDER_HETZNER_TOKEN=your-hetzner-dns-api-token
RELAYD_PROVIDER_HETZNER_ZONES=example.com
```

- **Scope**: `public`
- **Requires**: A Hetzner DNS API token (can be generated in the [Hetzner DNS Console](https://dns.hetzner.com/)).
